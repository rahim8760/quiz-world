import React from 'react';

const Options = ({qizoption}) => {
    console.log(qizoption);
    return (
        <div>
            <h4>{qizoption}</h4>
        </div>
    );
};

export default Options;