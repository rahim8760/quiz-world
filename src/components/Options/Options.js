import React from 'react';
import './Options.css'

const Options = ({qizoption}) => {
    return (
        <div className='option'>
            <h4>{qizoption}</h4>
        </div>
    );
};

export default Options;